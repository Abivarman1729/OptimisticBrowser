# Fixes applied

1. Removed the `optimistic_ai` test import mismatch.
2. Added real Android manifest + Flutter embedding activity files.
3. Added `flutter_launcher_icons` configuration.
4. Removed external search-engine fallback navigation; offline search is native-only.
5. Hardened navigation/download/intent policy and server result filtering.
6. Added backend rate limiting and request/category validation.
7. Added a canonical feature registry for the full requested capability set.
8. Added tab/session primitives, privacy snapshot service, search options and AI model/usage models.
9. Renamed the application/database branding from Optimistic AI to Optimistic Browser.
