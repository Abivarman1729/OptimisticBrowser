const form=document.querySelector('#searchForm'),input=document.querySelector('#query'),results=document.querySelector('#results');
const api=(window.OPTIMISTIC_API||'http://localhost:8787').replace(/\/$/,'');
form.addEventListener('submit',async e=>{e.preventDefault();const q=input.value.trim();if(!q)return;results.innerHTML='<p>Searching Optimistic backend…</p>';try{const r=await fetch(`${api}/api/search?q=${encodeURIComponent(q)}`);const data=await r.json();results.innerHTML=(data.results||[]).map(x=>`<article class="result"><a href="${escapeHtml(x.url)}" target="_blank" rel="noopener">${escapeHtml(x.title)}</a><p>${escapeHtml(x.description||'')}</p></article>`).join('')||'<p>No results.</p>'}catch(_){results.innerHTML='<p>Optimistic backend is offline.</p>'}});
document.querySelectorAll('.chip').forEach(b=>b.addEventListener('click',()=>{const a=b.dataset.action;if(a==='new'){input.value='';input.focus()}if(a==='private'){document.querySelector('#engineStatus').textContent='Private profile requested · native Android profile service controls storage isolation'}if(a==='clear'){results.innerHTML='';input.value='';}}));
function escapeHtml(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
